#!/run/current-system/sw/bin/bash
# I keep forgetting this command so I'm putting it in here.
# In the future this will probably also commit/push whatever I've changed.
sudo nixos-rebuild switch --flake /home/kaizen/nix-config#tower

